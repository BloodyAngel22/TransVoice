# TransVoice

TransVoice — лёгкая CLI-утилита для записи голоса через микрофон или использования аудиофайла и последующей транскрибации в текст с помощью модели Whisper.

## 🚀 Возможности

- Запись речи через микрофон (arecord)
- Использование любого аудиофайла с автоматическим преобразованием в WAV (ffmpeg)
- Распознавание речи моделью Whisper с поддержкой множества языков
- **Интерактивные настройки через команду `settings`** — выбор модели, языка, OpenVINO с опцией пропуска и визуальным отображением текущих настроек
- **Ускорение распознавания через OpenVINO (GPU)** — автоматическая проверка наличия файлов модели
- Возможность копирования текста в буфер обмена (xclip)
- Поддержка вывода детальной информации о WAV-файле
- **Мультиязычный интерфейс** — поддержка русского и английского языков
- **Сохранение настроек** — все настройки сохраняются в `settings.json` и используются при каждом запуске
- Простое использование через консоль

## ⚙️ Установка и запуск

1. Скачайте модель для распознавания речи: [Модели](https://huggingface.co/ggerganov/whisper.cpp/tree/main)
2. Переместите модель в папку `app/Models`.
3. (Опционально) Скачайте OpenVINO модели для ускорения распознавания: [OpenVINO Models](https://huggingface.co/bweng/whisper-large-v3-turbo-int8-ov/tree/main)
4. Переместите OpenVINO файлы (.xml и .bin с одинаковыми именами) в папку `app/OpenVinoModels`. OpenVINO будет автоматически использован, если файлы модели найдены.
5. Опубликуйте проект:
```bash
dotnet publish app.csproj -c Release -o publish
```

6. (Опционально) создайте символическую ссылку для глобального вызова:
```bash
sudo ln -s /путь/к/publish/app ~/.local/bin/transvoice
```

7. Настройте приложение (выберите модель, язык и OpenVINO):
```bash
transvoice settings
```

8. Запустите:
```bash
transvoice --help
```

## 🧩 Примеры использования
```bash
# Настроить приложение (выбор модели, языка, OpenVINO)
transvoice settings

# Записать голос и показать распознанный текст
transvoice --text

# Скопировать результат в буфер обмена
transvoice --clipboard

# Показать информацию о WAV-файле
transvoice --detail

# Использовать аудиофайл и преобразовать его в WAV перед распознаванием
transvoice --input /path/to/audio.mp3 --text
```

## ⚙️ Настройки

Приложение сохраняет настройки в файл `settings.json` в директории публикации. Доступные настройки:

- **Модель Whisper** — выбор модели из папки `Models`
- **Язык распознавания** — выбор языка (включая автоопределение)
- **OpenVINO** — выбор использования OpenVINO для ускорения (автоматически проверяет наличие файлов модели)

Команда `settings` предоставляет интерактивный интерфейс с:
- Визуальными галочками ✓ для текущих настроек
- Опцией "Skip" для пропуска изменения параметра
- Поддержкой русского и английского языков интерфейса

## Замечания

1. Работает и тестировался только на Linux.

2. Для записи и копирования должны быть установлены утилиты:
- arecord
- xclip

3. Модель Whisper загружается из папки `Models`. Выберите модель через команду `settings`.

4. Язык распознавания настраивается через команду `settings`. По умолчанию используется автоопределение.

5. OpenVINO модели должны находиться в папке `OpenVinoModels`. Для каждой модели нужны файлы .xml и .bin с одинаковыми именами. OpenVINO автоматически используется при наличии файлов модели.

6. Настройки сохраняются в файл `settings.json` и используются при каждом запуске приложения.

7. Интерфейс приложения поддерживает русский и английский языки в соответствии с настройками пользователя.
