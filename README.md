# TransVoice

TransVoice — лёгкая CLI-утилита для записи голоса через микрофон и последующей транскрибации аудио в текст с помощью модели Whisper.

## 🚀 Возможности

- Запись речи через микрофон (arecord)
- Распознавание речи моделью Whisper (ru)
- Возможность копирования текста в буфер обмена (xclip)
- Поддержка вывода детальной информации о WAV-файле
- Простое использование через консоль

## ⚙️ Установка и запуск

1. Скачайте модель для распознавания речи: [Модели](https://huggingface.co/ggerganov/whisper.cpp/tree/main)
2. Переместите модель в папку app/Models.
3. Если модель отличается от указанной в app/Constants/WhisperModelsConfig.cs, замените название модели.
4. Опубликуйте проект:
```bash
dotnet publish app.csproj -c Release -o publish
```

5. (Опционально) создайте символическую ссылку для глобального вызова:
```bash
sudo ln -s /путь/к/publish/app ~/.local/bin/transvoice
```

6. Запустите:
```bash
transvoice --help
```

## 🧩 Примеры использования
```bash
# Записать голос и показать распознанный текст
transvoice --text

# Скопировать результат в буфер обмена
transvoice --clipboard

# Показать информацию о WAV-файле
transvoice --detail
```

## Замечания

1. Работает и тестировался только на Linux.

2. Для записи и копирования должны быть установлены утилиты:
- arecord
- xclip

3. Модель по умолчанию использует ru-распознавание речи (можно изменить в билдере WhisperFactory - app/Core/WhisperDecoder.cs)
4. По умолчанию включён перевод текста на распознаваемый язык (можно отключить в билдере WhisperFactory - app/Core/WhisperDecoder.cs).
